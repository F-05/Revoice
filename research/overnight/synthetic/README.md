# Synthetic training data (overnight 2026-08-30)

TRAINING ONLY — never evaluation. Generated by scripts/synth_corrupt.py:
text-level pseudo N-best lists built by corrupting TRAINING-fold reference
sentences with confusion/deletion/insertion statistics mined from the SAME
fold's training speakers' real (A0, reference) alignments. Held-out speakers'
audio, transcripts and errors contribute nothing to their fold's synthetic
data. Each JSONL row carries full provenance (fold, source sample, generator
version, seed). Not a simulation of dysarthria — a ranker-robustness corpus.
